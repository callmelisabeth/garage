{% extends 'base.html.twig' %}

{% block title %}Modifier les utilisateurs
{% endblock %}

{% block body %}
	
	<main class="container blockbody">
		<section class="row">
			<div class="col-12">
				<div class="titlePage">
					<h1>Modifier les utilisateurs</h1>

					{{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

					<a href="{{ path('app_user_index') }}">Retour à la liste</a>

					{{ include('user/_delete_form.html.twig') }}

				</div>
			</div>
		</section>
	</main>
{% endblock %}
